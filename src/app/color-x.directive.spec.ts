import { ColorXDirective } from './color-x.directive';

describe('ColorXDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorXDirective();
    expect(directive).toBeTruthy();
  });
});
