import { ColorODirective } from './color-o.directive';

describe('ColorODirective', () => {
  it('should create an instance', () => {
    const directive = new ColorODirective();
    expect(directive).toBeTruthy();
  });
});
